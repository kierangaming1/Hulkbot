module.exports.run = (bot, message, args) => {
  let channel = message.channel
  
  channel.send("We don't steam ***[yet]***")
}

module.exports.help = {
  name: "stream",
  usage: ``,
  info: ""
}
 
